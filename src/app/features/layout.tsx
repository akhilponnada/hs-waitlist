import type { Metadata } from "next"

export const metadata: Metadata = {
    title: "Features",
    description:
        "Cognitive sectors with realistic decay, self-learning synapses, proactive recall, ten brain features, temporal reasoning, a knowledge graph, hybrid search and verified answers — the full Hypersave surface.",
}

export default function FeaturesLayout({ children }: { children: React.ReactNode }) {
    return children
}
